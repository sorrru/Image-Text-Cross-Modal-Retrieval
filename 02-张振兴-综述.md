# 论文笔记 02：张振兴、王亚雄《图文跨模态检索研究综述》(2024)

## 0. 和第一篇的关系（先定位一下）

第一篇（刘颖，2022）覆盖的是 2021 年前的工作，分类框架是"实值表示 vs 二进制表示"，核心矛盾是**准确度 vs 检索效率**。

这一篇（张振兴，2024）晚了两年，覆盖的是 Transformer 和大规模预训练（CLIP 之后）的时代，分类框架换成了**"图文对齐发生在哪个粒度层级"**：全局层级 → 区域层级 → 区域块（patch）层级。这条时间线正好补上了刘颖那篇里没来得及讲的最新进展——尤其是 CLIP、ALBEF 这些预训练模型。

这两篇拼在一起看，你就能看到这个领域一个很清晰的演化路径：**"分别编码、简单对比" → "检测物体、精细对齐" → "用 Transformer 把一切都端到端地学出来，靠海量数据预训练"**。这条路径就是你后面读英文顶刊时判断"这篇论文处于哪个阶段"的坐标系。

## 1. 这篇文章解决/总结的领域是什么？

同样是一篇综述，但视角更"工程化"。它没有像第一篇那样按"方法流派"分类，而是把整个建模过程拆成 **5 个步骤**：训练数据准备 → 数据输入形式设计 → 特征抽取机制选择 → 图文建模（对齐）方法选择 → 优化目标（损失函数）确立。

这个"拆步骤"的写法对你们后面自己写代码、复现模型特别有用——因为它相当于给了你一个"填空模板"：你做实验时，每一步都可以对照这 5 个环节，明确自己在这一步做了什么选择、为什么这么选。

## 2. 图文检索的基本任务是什么？

任务定义和第一篇完全一致：图片和文本因为处于不同的表示空间，没法直接比较相似度，所以要学一个"度量准则"，用它估计任意一张图和一句话之间的关联程度有多大，进而支持"以文搜图"和"以图搜文"。

这篇文章讲得更具体的是**主流模型结构**：几乎所有方法都基于"双塔结构"——一条分支专门编码图像，一条分支专门编码文本，两者最终被映射到同一个维度的共享子空间里，再通过损失函数（早期常用三元组损失，后期常用对比损失）来推动"配对样本靠近、不配对样本远离"。

## 3. 图文之间为什么难以直接匹配？

道理和第一篇一样（语义鸿沟），但这篇文章补充了一个很实用的细节：**不是所有信息都同等重要**。

- 图片里，背景往往不重要，前景物体才是匹配的关键；
- 句子里，介词、代词贡献很小，名词、动词、形容词贡献大。

这解释了为什么"把整张图片/整句话编码成一个全局向量"这种最简单的做法，天花板不高——因为它把重要信息和无关信息按相同权重混在了一起。这也是后面"区域级对齐"和"区域块级对齐"两条技术路线出现的直接动机：**想办法让模型知道该重点关注哪些局部信息**。

## 4. 三种对齐粒度的区别是什么？

这是这篇文章最核心的分类框架，建议做成一张表格记下来：

| 层级 | 怎么做 | 依赖的技术 | 优点 | 缺点 |
|---|---|---|---|---|
| **全局层级** | 整张图编码成一个向量，整句话编码成一个向量，直接比较这两个向量 | CNN（VGG/ResNet）+ RNN（LSTM/GRU） | 简单、训练快 | 精度天花板低，丢失局部细节 |
| **区域层级** | 先用目标检测（FasterRCNN）把图片切成一个个物体框，再计算每个物体框和每个单词的相似度，累加起来 | 目标检测 + 双向注意力机制 | 精度更高，能捕捉"这个词对应图里哪个物体" | 流程复杂（先检测再编码），难端到端训练；检测框大小不一，特征表达能力受限 |
| **区域块（patch）层级** | 不用目标检测，直接把图片切成规则的小方块（patch），像文本分词一样处理，整体扔进 Transformer | Vision Transformer + 文本 Transformer + 大规模预训练 | 可端到端训练，效果最好，能利用海量弱标注数据 | 需要巨量数据和算力做预训练 |

看这张表最重要的收获是：**技术路线的演进，本质上是"精度"和"工程可行性"之间的持续博弈**——区域级方法虽然精度提升了，但工程上很别扭（两阶段、非端到端）；Transformer 的出现同时解决了精度和端到端训练的问题，代价是需要大规模预训练数据和算力。这条主线正好呼应你自己"系统/工程"的关注点。

## 5. 预训练任务：ITC / ITM / MLM 分别是什么？

Transformer 时代的模型基本都靠这三种预训练任务的组合来训练：

- **图文对比学习（ITC）**：CLIP 用的就是这个。把一批图文对里"配对的"当正例，"不配对的"当负例，让正例的相似度远高于负例。这个任务的效果和训练时的批次大小强相关——批次越大，能对比的负例越多，效果通常越好，这也是为什么大厂训练 CLIP 类模型需要巨大的显存/算力。
- **图文匹配学习（ITM）**：把"这张图和这句话是否匹配"当成一个二分类问题来训练，通常作为辅助任务，用来补足 ITC 只看整体相似度、不看细粒度交互的短板。
- **跨模态遮挡语言建模（MLM）**：借鉴 BERT 的"完形填空"思路，随机遮住句子里的某些词，让模型结合图像信息把被遮住的词猜出来，逼着模型学习"词和图像区域的对应关系"。

这三个任务通常是组合使用的（比如 ALBEF 就同时用了这三种），不是二选一。

## 6. 我第一次见到、需要补的概念有哪些？

- **双塔结构（two-tower / dual-encoder）vs 单流结构（single-stream）**——patch 层级方法里提到的一个关键区分：图文各自独立编码到最后再融合，还是从一开始就拼在一起送进同一个 Transformer。这决定了训练效率和推理效率的巨大差异（双塔可以提前把候选库的向量算好存起来，单流通常不行）——**这一点对你做检索系统的工程实现极其重要，建议重点搞懂**。
- **困难样本挖掘（hard negative mining）**——VSE++提出的策略，思路是"不要用随机负例训练，要专门挑那些最容易被模型搞混的负例来训练"，后来被证明是提升效果很通用的技巧。
- **动量编码器（momentum encoder）/ 对比学习的负例队列**——ALBEF、MoCo 用来解决"显存放不下足够大的批次"这个工程问题的技巧，本质上是维护一个滞后更新的模型副本来扩充可用的负样本。
- **Recall@K 的具体计算方式**，以及为什么现在的论文几乎都只用它，不太用第一篇里提到的 MAP/F-score 了（可以带着这个问题去查一下，是否和数据集/任务设定的变化有关）。
- **AIGC 赋能预训练**这一节提到的 Stable Diffusion、BLIP2，如果你之前没接触过扩散模型和图像生成，可以先有个大致印象，不需要深挖。

## 7. 自查清单

- [ ] 我能不能不看表格，自己说出三种对齐粒度各自的核心思路和一个关键缺点？
- [ ] 我能不能解释清楚"双塔结构"和"单流结构"在**检索时的效率**上有什么本质区别？（提示：想想如果图片库有 100 万张图，双塔结构可以提前做什么，单流结构不行）
- [ ] 我能不能说清楚 ITC、ITM、MLM 三个预训练任务分别在训练模型的哪种能力？
- [ ] 综述提到"预训练数据和模型规模已经涨了几个数量级，但检索性能提升有限"，我能不能理解这句话想引出的问题是什么？
- [ ] 结合你自己的方向：文章提到的"经济的预训练方法"（并行化、模型压缩、数据蒸馏）这个未来方向，我能不能想到至少一个具体的、和"检索系统效率"相关的小切入点？

第三点开始，这份清单已经不只是"读懂论文"，而是在帮你为下一步——找 3 篇英文顶刊、想小创新点——做铺垫了。建议你先把"双塔 vs 单流"和"ITC/ITM/MLM"这两块吃透，因为你后面无论做检索效率优化还是模型本身的改进，几乎都绕不开这两个概念。
