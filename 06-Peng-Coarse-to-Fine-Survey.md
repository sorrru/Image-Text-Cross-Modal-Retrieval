# 论文笔记 06：Peng, Zheng & Liu《Cross-Modal Retrieval from Coarse-Grained to Fine-Grained Perspectives: A Survey》(JCST, 2026)

## 0. 这篇和前面五篇比,定位完全不同

前面五篇材料,不管中文英文,基本都在"图文检索(image-text retrieval)"这一个任务内部打转,顶多延伸到跨模态哈希。**这一篇的野心更大——它想把"跨模态检索"这整个大领域,用一个新的坐标轴重新切一遍:不是按方法(实值/哈希、全局/区域/patch),而是按"检索粒度"切成粗粒度(CCMR)和细粒度(FCMR)两大类。**

这意味着这篇论文会带你看到一些前五篇完全没提过的任务:**组合检索(Compositional Retrieval)、细粒度子类检索(Subcategory-Level Retrieval)、图像定位(Image Grounding)、视频时序定位(Video Temporal Grounding)**。这些名字对不上你之前的笔记很正常——它们是新任务,不是"图文检索"的另一种叫法。

**重要的是**:这篇论文的分类,正好和你导师之前帮你梳理的那份英文候选方向清单(CIR、fine-grained、efficient retrieval、domain-specific)基本对应上了——读完这篇,你手上关于"候选方向长什么样"的材料就完整了。

## 1. 核心坐标轴:CCMR vs FCMR

- **粗粒度跨模态检索(CCMR, Coarse-Grained Cross-Modal Retrieval)**:检索目标是"整个实例"——一整张图、一整段视频。你之前读的所有图文检索、图文匹配内容,都属于这一类。
- **细粒度跨模态检索(FCMR, Fine-Grained Cross-Modal Retrieval)**:又分两条线——
  1. **子类级检索(Subcategory-Level Retrieval)**:检索单位仍是整个实例,但要求区分同一大类下的细微子类(比如具体是哪种鸟、哪款车型),这类任务"类内差异小、类间差异也小",比普通图文检索难得多;
  2. **实例局部检索(Instance-Part Retrieval)**:检索目标比整个实例更小——比如图片里的某个区域,或视频里的某个时间片段。代表任务是**图像定位(Image Grounding)**和**视频时序定位(Video Temporal Grounding)**。

论文给了一个很清楚的判断标准:**CCMR 关心"全局语义匹配",FCMR 关心"局部对应关系,且往往要处理更严重的难负例问题"**。

## 2. 一个对你特别重要的观察:效率要求由检索范围决定

论文明确指出一个原因链条,直接呼应你已经关注的"Efficient/Large-Scale Retrieval"方向:

- **CCMR 需要从海量数据库里检索**,所以天然更看重效率——这也是为什么"哈希方法"的研究大量集中在 CCMR(尤其是图文检索)这一支,你在第一篇英文综述里读到的所有哈希方法,几乎都是为了给 CCMR 提速。
- **FCMR 里的实例局部检索(比如图像定位)搜索范围被限制在单张图片/单段视频内部**,所以可以承受 MLLM 那种计算量很大的"早融合"架构——这解释了为什么图像定位、视频时序定位领域大量使用 MLLM,而图文检索(CCMR)领域反而很少直接用 MLLM 做检索本身(太贵)。

**这条逻辑链把你之前分别读到的"哈希方法为什么重要"和"MLLM 为什么在某些任务里可行、某些任务里不可行"串起来了**——本质上就是"候选集大小决定了你能承受多重的计算"。

## 3. 新概念一:组合检索(Compositional Retrieval / CIR)

这就是你导师之前提到的 **Composed Image Retrieval**。论文的定义很清楚:用户提供一张参考图 + 一段文字修改要求,系统检索出"跟参考图相似但按文字要求做了改动"的目标图。

论文把它归入 CCMR(因为检索目标仍是整张图),但认为它是"检索范式的进化"——把单一模态查询(纯文字或纯图片)升级成了"图+文"的组合查询,能表达更精确、更灵活的检索意图。代表方法里出现了 CoVR、LinCIR 这些模型,都是最近一两年的工作。

## 4. 新概念二:子类级检索(Subcategory-Level Retrieval)

这是你导师清单里"Fine-Grained Retrieval"的准确定义:检索单位没变(还是整张图),但要求分辨同一大类下的细微差异(比如具体的鸟类品种、车型)。

论文提到一个很现实的困境:目前的基准数据集(CUB-200-2011 只有 200 种鸟、Stanford Cars 只有 196 种车型)覆盖的类别太少,而 CLIP 这类大模型虽然在"粗粒度、开放域"检索上很强,却缺乏区分这种"细微子类"所需要的判别力。论文把"如何打破对有限细粒度标注的依赖、让细粒度检索能扩展到任意类别"列为未来方向之一。

## 5. 新概念三:图像定位 / 视频时序定位

- **图像定位(Image Grounding)**:给一句自然语言描述,在**一张图片内部**定位并框出对应的区域(而不是从图片库里找图)。
- **视频时序定位(Video Temporal Grounding)**:给一句自然语言查询,在**一段视频内部**定位出对应的时间片段。

这两个任务都有"全监督"和"弱监督"两种设定——弱监督指的是训练时只有粗粒度的图文/视频文本标注,却要求模型在推理时具备定位到具体区域/片段的能力,这样可以大幅降低标注成本(标注精确的物体边界框非常耗时耗力)。

## 6. 未来方向(五个,论文给得比前几篇更具体)

1. **开放域子类级细粒度检索**:打破对有限细粒度标注数据集的依赖。
2. **统一多模态检索基础模型**:呼应 GPT-5/Gemini 这类通用多模态基础模型的趋势,思考"检索"是否会被整合进通用大模型能力,而不再是一个独立训练的专门模型。
3. **数据为中心的范式(Data-Centric Revolution)**:论文认为随着模型架构逐渐成熟,继续靠"改模型结构"拿到的性能提升越来越有限,未来的提升更多要靠数据筛选、去噪、多样性设计,甚至用大模型合成高质量的图文对来补充稀缺/嘈杂的标注。
4. **用户为中心的检索(User-Centric Retrieval)**:强调交互式检索——根据用户的反馈动态调整、修正检索结果,而不是"一次查询、一次返回"的静态模式。
5. **新兴模态**:具身智能/机器人(视觉+动作/声音对齐)、自动驾驶(相机图像+激光雷达点云对齐)、医学(生物信号如脑电图+医学影像对齐)。

**第3点(数据为中心)特别值得记住**——它其实是对你之前从第二篇英文综述读到的"骨干选择比算法设计更重要"这个结论的进一步延伸:如果连"选更强的骨干"都快摸到天花板了,那下一步的性能提升就要靠"喂给模型更好的数据",而不是更精巧的算法设计。这对你规划自己的小项目也是一个诚实的参考——如果算力和模型规模都做不大,数据质量(哪怕只是筛选、清洗训练数据)可能是一个性价比很高的着力点。

## 7. 需要补的概念

- **难负例(hard negative)的"更严重"版本**——论文强调 FCMR 面临的难负例问题比 CCMR 更严重,因为子类之间"类内差异大、类间差异小",这跟你在中文第三篇(EISIN)里已经理解的"难分样本"是同一个概念,只是在细粒度场景下更极端。
- **早融合 vs 晚融合(early fusion vs late fusion)**——论文用这对概念解释为什么 MLLM 能用在图像定位/视频时序定位(候选范围小,能承受早融合的计算量),却不能直接用在大规模图文检索(候选范围太大)。这组概念值得专门查一下定义,是理解"检索效率"话题的一个重要工具。
- **弱监督设定(weakly-supervised setting)**——在图像定位、视频时序定位、甚至子类级检索里反复出现,指用更便宜的标注(整图/整句配对)去训练模型完成更精细的任务(定位到具体区域/时间段),值得理解它解决的是"标注成本"这个现实问题。

## 8. 自查清单

- [ ] 我能不能用一句话说清楚 CCMR 和 FCMR 的核心区别,并举出各自的代表任务?
- [ ] 我能不能解释清楚,为什么"候选集大小"决定了一个任务能不能用 MLLM、要不要用哈希方法?
- [ ] 我能不能说清楚"组合检索"和普通的"以文搜图"有什么本质不同?
- [ ] 结合你的方向:如果你想做的还是"输入文字,在图库里找图"(标准的图文检索/CCMR),这篇论文里跟你最相关的是哪几节?(提示:3.1 节 Image-Text Retrieval 部分,以及未来方向里的"数据为中心"和"统一基础模型"两条)

## 9. 三篇英文综述读完,现在可以做一个总的盘点

到这里,你手上已经有了一份相当完整的地图:
- **Han et al. 2024**:跨模态检索整体框架 + 哈希方法深度剖析(对应"Efficient/Large-Scale Retrieval"候选方向)。
- **Dong & Wei 2026**:图文匹配的三大技术路线(FE/CMA/VLP)+ 明确的"VLP最强、骨干比算法重要"结论。
- **Peng et al. 2026(这一篇)**:粗粒度/细粒度的宏观坐标轴,补上了组合检索(CIR)、子类级检索(Fine-Grained)、图像/视频定位等你导师清单里提到但前面没细讲的方向。

三篇拼起来,你现在应该能对导师给你的候选清单(CIR、Efficient Retrieval、Fine-Grained、Domain-Specific)里的每一个都说出"它具体是什么、难点在哪、跟标准图文检索是什么关系"——这已经足够支撑你和老师认真讨论"第四篇精读方向该选哪个"这个问题了。
